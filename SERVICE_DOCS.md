# PM Master: 서비스 설계 및 운영 가이드 (Service Design & Operations)

본 문서는 PM Master 애플리케이션의 기술적 설계 구조와 안전한 운영을 위한 관리 지침을 제공합니다.

---

## 1. 시스템 개요 (System Overview)

- **서비스 명**: PM Master (IT Project Management & Automation Hub)
- **주요 목적**: IT 프로젝트의 다단계(3단계) 진행 현황을 시각화하고WBS 단위의 세부 관리를 지원하는 경량 관리 도구
- **기술 스택**: 
  - Frontend: React 18, TypeScript, Vite
  - Styling: Tailwind CSS
  - Charts: Recharts
  - Animation: Framer Motion
  - Icons: Lucide React

---

## 2. 데이터 아키텍처 (Data Architecture)

### 2.1 데이터 계층 구조 (3단계)
1. **Depth 1 (Status 1)**: 프로젝트 대분류 상태 (착수, 수행, 완료 등)
2. **Depth 2 (Status 2)**: 공정별 상세 단계 (기획, 디자인, 개발 등)
3. **Depth 3 (WBS Tasks)**: 실제 수행되는 개별 작업 단위

### 2.2 메타데이터 (Constants)
- 프로젝트 상태(Status) 리스트는 `constants.ts`에 정의되어 있으며, 사용자가 설정(Settings) 메뉴를 통해 동적으로 확장할 수 있습니다.
- 로컬 스토리지 또는 JSON 백업/복구를 통해 영속성을 유지합니다.

---

## 3. 핵심 설계 원칙 (Design Principles)

### 3.1 드릴다운 분석 (Drill-down Analysis)
- 대시보드의 파이 차트(1단계) -> 상세 공정(2단계) -> WBS 리스트(3단계)로 이어지는 연동 필터링 구조를 가집니다.
- `useMemo`를 통한 고성능 데이터 필터링으로 대규모 프로젝트 리스트에서도 끊김 없는 사용자 경험을 제공합니다.

### 3.2 안전한 데이터 관리
- **백업(Export)**: 모든 프로젝트 데이터와 설정 리스트를 타임스탬프가 포함된 단일 JSON 파일로 추출합니다.
- **복구(Import)**: 기존 데이터를 완전히 덮어쓰거나 선택적으로 병합할 수 있는 구조를 제안합니다.

---

## 4. 운영 가이드 (Operating Guide)

### 4.1 초기 설정 프로세스
1. 사이드바의 **Settings** 메뉴로 이동합니다.
2. 조직의 워크플로우에 맞는 **1단계 상태**와 **2단계 상세 공정**을 등록합니다.
3. **Project Management** 메뉴에서 첫 번째 프로젝트를 등록합니다.

### 4.2 일일/주간 관리 프로세스
- **업데이트**: 각 프로젝트 상세 페이지에서 WBS 항목의 완료 여부를 체크합니다.
- **모니터링**: 대시보드의 '3단계 WBS 모니터링' 섹션을 통해 누락된 작업이 없는지 확인합니다.
- **보고**: 필요한 필터(당월, 특정 상태 등)를 적용한 후 **CSV 내보내기**를 통해 엑셀용 보고서를 생성합니다.

### 4.3 주의 사항
- **브라우저 캐시**: 본 버전은 클라이언트 기반 데이터 저장 방식을 사용하므로 브라우저 캐시를 삭제하면 데이터가 손실될 수 있습니다. 주기적으로 설정 메뉴에서 JSON 백업을 권장합니다.
- **날짜 형식**: 모든 날짜는 `YYYY-MM-DD` 형식을 표준으로 사용합니다.

---

## 5. 향후 확장 계획 (Roadmap)

- **Firebase 연동**: 멀티 유저 협업 및 실시간 데이터 동기화 기능 추가.
- **Gantt 차트**: 기간별 일정을 시각적으로 나타내는 간트 차트 뷰 도입.
- **AI 리포트**: 프로젝트 진행률을 분석하여 지연 위험이 있는 프로젝트를 자동으로 감지.
