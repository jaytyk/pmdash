# PM Master: AI 기반 프로젝트 관리 허브 재구현 프롬프트 가이드

이 문서는 다른 AI 코딩 어시스턴트(Claude, GPT-4, Cursor 등)를 사용하여 현재의 **PM Master**와 동일한 기능과 디자인을 가진 애플리케이션을 처음부터 구축하기 위한 상세 프롬프트를 제공합니다.

---

## 1. 초기 프로젝트 설정 프롬프트

> "React 18+, Vite, TypeScript, Tailwind CSS를 사용하는 현대적인 프로젝트 관리 대시보드 앱을 제작해줘. 애니메이션을 위해 `framer-motion`을 사용하고, 차트 구현을 위해 `recharts`, 아이콘은 `lucide-react`를 사용해. 디자인 시스템은 최신 SaaS 대시보드 스타일(Inter 폰트, Slate/Blue 컬러 테마, 부드러운 그림자와 둥근 모서리)을 따라야 해."

## 2. 데이터 구조(Type) 정의 프롬프트

> "애플리케이션의 핵심 데이터 구조인 `Project` 타입을 정의해줘. 각 프로젝트는 다음 필드를 가져야 해:
> - `id`: UUID (string)
> - `name`: 프로젝트명 (string)
> - `manager`: 담당 PM (string)
> - `status1`: 1단계 상태 (예: 착수, 수행, 완료, 보류 등)
> - `status2`: 2단계 상세 공정 (예: 기획, 디자인, 퍼블리싱, 개발, QA 등)
> - `startDate`, `endDate`: ISO 날짜 문자열
> - `description`: 상세 설명 (string)
> - `tasks`: WBS 항목들의 배열 (각 항목은 `id`, `title`, `isCompleted`, `parentStatus2` 등을 포함)"

## 3. 핵심 기능 구현 프롬프트

### A. 대시보드(Dashboard) 기능
> "메인 화면인 대시보드 컴포넌트를 만들어줘. 다음 기능이 포함되어야 해:
> 1. **드릴다운 필터링**: 
>    - Recharts의 Pie Chart를 사용하여 '1단계 상태' 분포를 시각화해.
>    - 차트 조각을 클릭하면 해당 상태의 프로젝트만 필터링되어야 해.
>    - 2단계 상세 공정(리스트 형태)을 클릭하여 다중 선택 필터링이 가능해야 해.
> 2. **WBS 모니터링 (3rd Depth)**: 
>    - 현재 필터링된 프로젝트들에 속한 모든 WBS 작업들을 진행률과 함께 카드 형태로 요약해서 보여줘.
> 3. **기간 필터**: 오늘 기준 당월, 분기, 금년, 사용자 정의 기간 필터를 구현해.
> 4. **CSV 내보내기**: 현재 필터링된 리스트를 BOM이 포함된 CSV 파일로 다운로드하는 기능을 추가해."

### B. 프로젝트 관리 및 상세뷰
> "프로젝트 목록을 관리하고 개별 프로젝트를 추가/수정할 수 있는 폼과 모달을 구현해. 상세뷰에서는 해당 프로젝트의 WBS(작업 리스트)를 체크박스로 완료 처리할 수 있어야 하며, 전체 진행률이 실시간으로 계산되어 표시되어야 해."

### C. 설정 및 데이터 백업
> "설정 화면에서 '1단계 상태'와 '2단계 상세' 리스트를 사용자가 직접 커스터마이징할 수 있게 해줘. 또한 전체 데이터를 JSON 파일로 백업하고 다시 불러오는(Restore) 기능을 구현해."

## 4. UI/UX 디자인 가이드 프롬프트

> "UI는 다음과 같은 규칙을 지켜줘:
> - **레이아웃**: 좌측 사이드바와 우측 컨텐츠 영역으로 구분.
> - **컴포넌트 스타일**: `bg-white`, `border-slate-200`, `rounded-2xl`, `shadow-sm`을 기본으로 사용.
> - **애니메이션**: 페이지 전환 시 `framer-motion`의 `opacity`와 `y`축 이동을 사용하여 부드럽게 등장시켜줘.
> - **반응형**: 모바일 기기에서도 테이블이 가로 스크롤로 깨지지 않도록 처리하고, 차트 크기가 유연하게 조절되게 해줘."
